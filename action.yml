# action.yml
name: 'Release Node Package'
description: 'This action releases a Node Package'
inputs:
  VERSION:
    description: 'Release Version ( PATCH | MINOR | MAJOR )'
    required: true
    default: 'PATCH'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.VERSION }}